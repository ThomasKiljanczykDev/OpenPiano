name: Publish store listing

# Uploads the Play listing (title, descriptions, screenshots, feature graphic, icon) as a draft:
# nothing ships until "Send for review" in Play Console.
# fastlane/metadata is the source of truth; this overwrites Console edits.
# Release notes are cd-release-notes.yml: they need release permission the listing does not.

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Validate only: runs the whole Play edit, then discards it"
        type: boolean
        default: true
      track:
        description: "Track holding the release to attach the edit to"
        type: choice
        default: internal
        options: [internal, alpha, beta, production]

permissions:
  contents: read

# Concurrent Play edits conflict on commit.
concurrency:
  group: play-edit
  cancel-in-progress: false

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/android-setup

      - name: Render screenshots
        working-directory: android
        run: ./gradlew :tools:gplay-screenshots:updateDebugScreenshotTest

      - name: Export screenshots
        run: android/tools/gplay-screenshots/export_to_fastlane.sh

      - name: Validate images
        run: python3 fastlane/validate_metadata.py --expect-images

      - uses: actions/upload-artifact@v7
        with:
          name: store-listing-screenshots
          path: fastlane/metadata/android/*/images/phoneScreenshots/
          if-no-files-found: error

  publish:
    needs: render
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: production
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          name: store-listing-screenshots
          path: fastlane/metadata/android

      - uses: ruby/setup-ruby@v1
        with:
          working-directory: fastlane
          bundler-cache: true

      # supply resolves a track release even for an images-only edit; the version code must exist on that track.
      - name: Resolve version code
        id: version
        uses: ./.github/actions/app-version

      - name: Check Google Cloud configuration
        run: |
          missing=
          [ -n '${{ vars.GCP_WI_PROVIDER }}' ] || missing="$missing GCP_WI_PROVIDER"
          [ -n '${{ vars.GCP_PLAY_SA }}' ] || missing="$missing GCP_PLAY_SA"
          if [ -n "$missing" ]; then
            echo "::error::Missing variable(s):$missing. See docs/RELEASING.md."
            exit 1
          fi

      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GCP_WI_PROVIDER }}
          service_account: ${{ vars.GCP_PLAY_SA }}
          create_credentials_file: true

      - name: Upload listing to Play
        working-directory: fastlane
        timeout-minutes: 20
        env:
          SUPPLY_UPLOAD_MAX_RETRIES: "5"
        run: |
          exec bundle exec fastlane supply \
            --package_name dev.thomas_kiljanczyk.openpiano \
            --metadata_path metadata/android \
            --json_key '${{ steps.auth.outputs.credentials_file_path }}' \
            --track '${{ inputs.track }}' \
            --version_code '${{ steps.version.outputs.version_code }}' \
            --skip_upload_changelogs \
            --skip_upload_apk --skip_upload_aab \
            --changes_not_sent_for_review \
            ${{ inputs.dry_run && '--validate_only' || ' ' }}

      - name: Summarise
        run: |
          if [ '${{ inputs.dry_run }}' = 'true' ]; then
            echo '## Dry run: nothing was written to Play' >> "$GITHUB_STEP_SUMMARY"
          else
            echo '## Listing staged as a draft. Press "Send for review" in Play Console.' >> "$GITHUB_STEP_SUMMARY"
          fi
